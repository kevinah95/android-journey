on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

name: release-please

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: google-github-actions/release-please-action@v3
        with:
          release-type: simple
          package-name: android-journey
          bump-minor-pre-major: false
          bump-patch-for-minor-pre-major: false
          extra-files: |
            README.md